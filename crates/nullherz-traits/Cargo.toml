[package]
name = "nullherz-traits"
version = "0.1.0"
edition = "2024"

[features]
test-utils = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde-big-array = "0.5"
libc = "0.2"
dyn-clone = "1.0"
